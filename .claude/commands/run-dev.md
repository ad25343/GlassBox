Start the GlassBox development environment:
1. Check `.env` exists and has required keys (ANTHROPIC_API_KEY, OPENAI_API_KEY, GOOGLE_API_KEY)
2. Run `uvicorn backend.main:app --reload --port 8000`
3. Report the local URL and any startup warnings
